# HNTV (Hard Negative Temporal Verification) — 작업 노트

SNU AI Challenge 2026 출품 프로젝트. 4장의 비디오 프레임 + 설명 문장을 입력으로 받아
"이 프레임 순서가 사건의 올바른 시간 순서인가?"를 VLM(+LoRA)으로 판별해 정답 순열을 맞히는 모델.

---

## 0. 현재 상태 (대시보드 — 상세 근거는 각 절 링크 참고, 여기는 항상 짧게 유지)

### 최고 제출
**v14 (Qwen3-VL-8B + ListwiseSoftmaxLoss), LB 0.90052** — `v14_listwise_softmax/checkpoints/best_v14`,
epoch5, val=0.5987. v8 레시피(LoRA r=128/alpha=256, LR=5e-5, hard_negative.py K=7)를 그대로 두고 loss만
8-way joint softmax(ListwiseSoftmaxLoss)로 교체한 단일 변수 실험 — 자세한 설계·근거는 §4 표의 v14 행 참고.

### 순위 (LB 기준)
| 순위 | 버전 | LB | 비고 |
|---|---|---|---|
| 1 | **v14** | **0.90052** | 현재 최고 제출 |
| 2 | v8 = v11 | 0.89528 | v11은 v8과 우연히 동점(4-1절) |
| 3 | v12 | 0.89354 | vision joint, val은 높았으나 LB는 낮음 |
| 4 | v10 | 0.88481 | noun-loss, val 역대 최고였으나 LB는 낮음 |
| 5 | v6.5 | 0.87260 | |
| 6 | v6 | 0.87085 | |
| 7 | v4 | 0.83944 | |
| 8 | v3 | 취소 | 24h 한도 초과 |
| 9 | v9(재보정) | 0.79232 | |
| 10 | v1 | 0.79057 | |
| 11 | v2 | 0.76963 | |
| 12 | v2.5 | 0.71553 | TTA flip 급락 |

v13, v15는 프롬프트/구조 실험이라 자체 LB 제출 없음(v13은 재학습 없이 A/B만, v15는 §4 참고 — 다수결
예측상 v14보다 낮을 것으로 보여 제출 보류 중).

### 최근 확정된 결론 (재탐색 방지용 — 전부 §4/§5에 상세 근거 있음)
- **vision encoder 추가 학습(v11, v12) — 폐기 확정.** 두 가지 다른 격리 방식 모두 v8을 못 넘음.
- **프롬프트 nudge(v13: n_noun grounding, adjswap-hint) — 폐기 확정.** 생성 없이 지시문만 추가하는
  방식은 2회 이상 무반응/실패로 확인됨. 진짜 생성형 CoT도 v1/v4에서 이미 실패(5-9절).
- **LoRA 변형(DoRA/PiSSA/rsLoRA/LoRA+) — 폐기 확정.** DoRA는 모든 rank에서 OOM, 나머지는 붕괴하거나
  v8보다 저조.
- **K(hard negative 개수) 확장 — 물리적으로 불가능 확정.** 그리드서치+실제 accelerate 검증(2026-07-08)
  으로 K=7(v8/v14와 동일)이 이 하드웨어의 진짜 한계임을 확인. 상세는 §5-11.
- **v15(동적 하드 네거티브 뱅크) — 애매/보류.** K는 그대로 두고 d1 보너스 슬롯만 "전 epoch에 모델이
  헷갈렸던 negative"로 교체하는 방식으로 구현·학습까지 완료했으나, val은 v14와 동률(epoch3=0.5966)에
  그쳤고 다수결 일치율은 v14보다 낮게 나와(92.19% vs 94.99%) LB 예측치가 v14보다 낮음(~0.874). 제출은
  보류 중 — §4 v15 행 참고.
- **화면 자막/타이틀카드 발견 — 아직 미실행.** train 내부에서 순서를 알려주는 화면 텍스트가 일부
  샘플에 존재함을 확인(§5-10). tesseract 사전 추출 + 조건부 프롬프트 삽입 설계는 돼 있으나 미착수.

### 다음 액션 우선순위 (상세 체크리스트는 §7)
1. **RTX 3090 단일 GPU 최종 실행 검증** — 대회 시작 이래 한 번도 안 함, 남은 최대 리스크.
2. 화면 자막 OCR 프롬프트 실험 (저비용, 기대효과는 작음)
3. d=1 top-2 pairwise tie-break (미착수)
4. v15를 제출할지, 아니면 v14를 그대로 최종으로 갈지 결정 필요

---

## 1. 태스크 & 현재(v6) 모델 구조

- Base: Qwen3-VL-8B-Instruct + LoRA (r=64, alpha=128, dropout=0.05, target: q/k/v/o_proj, gate/up/down_proj)
- 입력: 4장 프레임(특정 순서로 배열) + Sentence → "Yes"/"No" 답변
- 점수: log P(Yes) − log P(No) (log-odds ratio)
- Hard Negative: 켄달거리 d=1~6 전 구간 커버, distribution-informed 샘플링
  (`SAMPLE_COUNTS = {1:2, 2:1, 3:1, 4:1, 5:1, 6:1}`, 1 pos + 7 neg = 8 samples/group = 1 forward pass)
- No_ordering 샘플(15.5%)도 Ordered와 동일하게 hard-negative 처리 (구버전의 "positive만 5배 증강" 방식 폐기)
- Loss: AdaptiveDistanceLoss — distance별 EMA로 실시간 난이도 추적, softmax(ema × temperature)로 가중치 산출
- Inference: 24개(4!) 순열 전부 스코어링 → 최고 점수 순서를 정답으로 제출

버전별 상세 설계 변천은 4절, 세부 근거는 5절 참고.

---

## 2. 대회 규칙 요약 (전문은 부록 참고)

### 핵심 제약
- **외부 데이터 금지**, 제공된 학습 데이터만 사용
- **오픈소스 모델 가중치 공개일 2026-05-31 이전**만 허용
- **모델 앙상블 금지** (동일 모델 여러 파인튜닝 후 조합도 금지)
- **생성형 모델을 이용한 데이터 생성/변형 금지** (전처리 목적 외부 API는 3만원 이하로 허용)
- 양자화/LoRA 등 경량화, CoT/Multi-turn/TTA 등 추론 전략은 허용 (24시간 추론 제한 내)
- **데이터 누수 금지**: 평가 데이터 특성을 분석해 학습 설계에 반영하는 것도 금지 (회색지대 주의)
- **최종 제출: RTX 3090 1개(24GB)에서 실행 가능**해야 함
- **채점: Exact Match만, 부분점수 없음** (예선 리더보드는 test의 70%만 반영)
- 제출 1일 2회 제한

### 준수 현황 체크리스트
| 규칙 | 상태 | 비고 |
|---|---|---|
| 외부 데이터 금지 | ✅ | 제공 데이터만 사용 |
| 모델 공개일 (2026-05-31 이전) | ✅ | TPRU-7B 2026-02-12, Qwen3-VL 2025-10-15 모두 충족 |
| 앙상블 금지 | ✅ | 단일 모델만 사용 |
| 생성형 모델 aug 금지 | ✅ | 관련 코드 전부 비활성화/미사용 |
| 데이터 누수 | ⚠️ 과거 회색지대 | v1-실험1/v2에서 평가 데이터 특성을 설계에 참고한 적 있음(v3 이후 완전 제거, 해당 제출은 이미 반영됨). v4/v6는 train 내부 val split만 사용해 클린. |
| RTX 3090 단일 GPU 최종 실행 | ⚠️ **미검증** | 현재 전부 4-GPU DDP로 개발 중. **최종 제출 전 반드시 1-GPU 단독 실행 검증 필요** (8B급 모델은 7B보다 여유가 더 빡빡할 것으로 예상) |
| 모델 크기 80GB 이하 | ✅ | bf16 8~9B + LoRA ~350MB 정도, 여유 있음 |
| 추론 24시간 이내 | ✅ | DDP 기준 수십 분, 1-GPU 환산해도 여유 |

---

## 3. 데이터 EDA 핵심 요약 (전체 상세는 `EDA.md` 참고)

### 기본 구조
- train 9,535개 / test 819개, 각 4장 이미지 + Sentence + Answer(순위 배열)
- No_ordering=True(15.5%, 1478개): 프레임이 이미 정답 순서 → Answer는 항상 `[1,2,3,4]`
- No_ordering=False(84.5%): Answer 24개 순열에 거의 균등 분포 (특정 순열 편향 없음)

### 정답 순열의 켄달거리(τ) 분포 — 어려운 케이스가 고르게 분포
| τ | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| 비율 | 13.1% | 21.3% | 25.8% | 21.9% | 13.3% | 4.5% |

### 문장 길이 축 (★ 미해결 약점)
train/val 내부에서 "문장 길이 ↑ → 정확도 ↑" 상관관계(r=0.376)를 확인했으나, **이 축을 겨냥한 개선은
아직 하나도 시도 안 함** — 다음 실험 후보 1순위.

### 이미지 시각적 유사도 EDA (v6 해상도 결정에 직접 사용)
- 원본 이미지 longest side 중앙값 640px, 90%가 448px보다 큼 → 낮은 MAX_IMAGE_SIZE는 실제로 다운스케일을 일으킴
- **그러나** gap(시간 간격)별 인접 프레임 유사도 효과(Cohen's d)가 **32px~448px 전 구간에서 0.303~0.306으로 거의 동일**
  → d=1(인접 스왑) 오답의 원인이 되는 시각적 유사도 신호는 **해상도와 무관** (큰 그림 수준 차이, 미세 디테일 아님)
  → **해상도를 올려도 이 특정 문제는 해결 안 됨** — 512px 시도했으나 OOM 리스크만 있고 실익 없어 448px 유지 (6절 참고)

---

## 4. 버전별 요약

| 버전 | Base | 핵심 설계 | 결과 | 판정 |
|---|---|---|---|---|
| v1 | Qwen2-VL-7B | LoRA r=32, adj-swap 3종 hard neg | LB 0.79057 | 초기 베이스라인 |
| v1-실험1 | Qwen2-VL-7B | 짧은 문장 오버샘플링 | LB 0.76963 | ↓ 실패 (분포 시프트 방향 착각) |
| v2 | **TPRU-7B** | adj-swap 3 + reverse 1, ListNet | LB 0.81849 | ✅ base 교체 효과 확인 |
| v2.5 | TPRU-7B(v2 체크포인트) | +TTA flip +pairwise +560px, 추론만 | LB 0.71553 | ↓ 급락 (flip이 방향 정보 파괴) |
| v3 | TPRU-7B | 23개 negative 전부 사용 | 취소 | 145h/5epoch로 24h 파티션 초과 |
| v4 | TPRU-7B | SAMPLE_COUNTS 전거리 커버 + AdaptiveDistanceLoss | LB 0.83944 | ✅ 이전 최고 |
| v5 | Qwen3-VL-8B | listwise 순열 직접 생성(SFT), hard-negative 없음 | val 0.4475 | ↓ v4 미달로 폐기 (원인: 8절) |
| v6 | Qwen3-VL-8B | v4 레시피 + base 교체 + temp 튜닝 + No_ordering 통합 | LB 0.87085 | ✅ 이전 최고 (epoch4, val=0.5861) |
| v6.5 | Qwen3-VL-8B | v6 레시피 + hard negative를 학습 시작 전 고정하지 않고 매 epoch(사실상 매 `__getitem__`)마다 live 재샘플링 + EPOCHS 5→10 확장. 10epoch 궤적: 0.5336→0.5504→0.5882→0.5819→**0.6029(best,epoch5)**→0.5756→0.5924→0.5966→0.5819→0.5966 — v6처럼 정점 후 단조 하락하지 않고 5epoch 이후로도 진동하며 개선 여지 지속. adj_swap_fail 최저 51(v4 60, v6 66 대비 최고) | LB 0.87260 | ✅ v6(0.87085) 대비 +0.175pp. v6 자체는 최초 추론시 `inference.py`의 `MAX_SIZE`가 512로 남아 학습(448px)과 불일치했던 버그를 발견·수정(job 226453)해서 재추론 후 제출한 이력 있음 |
| v7_nounloss | Qwen3-VL-8B | v6.5 + AdaptiveDistanceNounLoss(n_nouns 버킷축) | val epoch2=0.5336 | ↓ v6.5 대비 격차 확대(−3.57pp)로 epoch2 중단. adj_swap_fail만은 v6.5보다 나았음(64 vs 67) — noun_temperature=6.0 과도 추정, 재시도시 2~3 권장 |
| v7_framediff | Qwen3-VL-8B | frame-diff 이미지를 입력에 추가 | - | 보류 — VRAM OOM으로 중단. EDA §10-1(min_adj_rmse가 정확도 예측력 약함) 발견 이후 우선순위 하향, 재시도 안 함 |
| v8 | Qwen3-VL-8B | v6.5 + LoRA r=64→128(alpha=128→256, 비율 2.0 유지) + LR 1e-5→5e-5(논문 "LoRA Learns Less and Forgets Less" 근거, LLM-only LoRA는 full-finetuning보다 한 자릿수 높은 LR 필요) + EPOCHS 10→5 | **LB 0.89528** | ✅ **현재 최고** (epoch1~3까지 v6.5 앞서다 epoch4부터 과적합으로 역전, best=epoch3 val=0.5924 체크포인트로 제출) |
| v9_dora(폐기) | Qwen3-VL-8B | v8 + DoRA(use_dora=True) | - | ↓ r=128~64 전부 OOM(rank 무관 고정 VRAM 오버헤드 ~3.2GB, forward마다 d×k 가중치 재구성) 확인되어 학습 시도조차 못 함 |
| v9(PiSSA+rsLoRA+LoRA+, 폐기) | Qwen3-VL-8B | v8 + PiSSA(init_lora_weights="pissa_niter_16") + rsLoRA(use_rslora=True, alpha=256 유지) + LoRA+(lora_B에 lora_A의 16배 LR) | val epoch1=0.1618 | ↓↓↓ **참사** — step1300부터 loss가 ln(2)=0.693(이진분류 완전 붕괴)에 고정, val 16.18%(v8 55.04%)로 폭락. rsLoRA effective scale(alpha/sqrt(r)≈22.6, 기존 2.0의 11배)과 LoRA+ B쪽 LR(8e-4, A의 16배)을 동시 도입한 게 과했던 것으로 추정 — 여러 변경을 한 번에 묶어 원인 미분리(5-4 교훈 재현) |
| v9(PiSSA+rsLoRA, α=16 재보정) | Qwen3-VL-8B | v8 + PiSSA + rsLoRA(alpha=16 고정, effective scale≈1.41로 v8의 2.0과 비슷하게 재보정, LoRA+는 제외) | val epoch5(best)=0.5567 | ↓ 안정적으로 수렴(구조적 붕괴 없음)했지만 v8(0.5924) 대비 낮음, **LB 0.79232로 v8보다 크게 저조**, 폐기 |
| v10_nounloss_retry | Qwen3-VL-8B | v8 레시피 + AdaptiveDistanceNounLoss(n_nouns 버킷 축, temperature=2.5로 재보정 — v7의 6.0은 과했음) | **val epoch4(best)=0.6071 (역대 최고 val)** | val은 v6.5(0.6029)·v8(0.5924) 다 능가했으나 **LB는 0.88481로 v8(0.89528)보다 낮음** — val↔LB 불일치가 v8 때(낮은 val, 높은 LB)와 반대 방향으로 재현됨(4-1절 참고) |
| v11_vision_lora | Qwen3-VL-8B | v8 체크포인트(best_v8, LLM LoRA r=128)를 base에 `merge_and_unload()`로 영구 병합해 LLM을 완전 고정, 그 위에 vision encoder 마지막 4블록(23~26)에만 새 LoRA(r=64, alpha=128)를 추가해 vision만 학습 ("freeze LLM + vision-only LoRA" 전략) | best epoch4=0.5903, last epoch5=0.5882 | val 기준 5 epoch 내내 **v8의 병합 시작점(0.5924)을 한 번도 못 넘음**. **LB 0.89528로 v8과 완전 동점** — 근데 실제 예측 819개 중 35개(4.3%)가 v8과 다름(§4-2), 공개 70% 구간에서 득실이 우연히 상쇄된 것으로 확인됨(진짜 동일 성능 아님). vision LoRA 추가 학습 폐기 |
| v12_vision_joint | Qwen3-VL-8B | 베이스 모델에서 LLM LoRA(r=128, v8과 동일 설정)와 vision LoRA(마지막 3블록, r=64/alpha=128)를 **처음부터 동시학습** ("joint from scratch" 전략, v11의 대조군). vision LoRA에 LLM(5e-5)보다 낮은 LR(2.5e-5) 분리 적용 | epoch1=0.5714, epoch2=0.5819, epoch3=0.5819, **epoch4=0.6092(290/476, 당시 역대 최고 val)**, epoch5=0.6029 | val은 v8(0.5924)·v10(0.6071) 다 능가했으나 **LB는 0.89354로 v8(0.89528)보다 낮음** — v10과 같은 방향의 val↔LB 불일치 재현. No_ordering 별도 정확도가 epoch2/3 동일 overall(0.5819)인데도 55/77→49/77로 갈리는 등, overall exact_match 하나로는 안 보이는 트레이드오프 확인(4-2절 참고). **결론: v11(다른 격리 방식)과 함께 "vision encoder 학습은 도움 안 됨"으로 최종 폐기 확정** |
| v13_noun_grounding_cot | Qwen3-VL-8B | 재학습 없음, best_v8에 프롬프트만 3종 A/B(baseline / adjswap-hint / n_noun grounding). EDA §10-2의 n_nouns 발견을 프롬프트 지시문으로 처음 전환 시도 — 단, 실제 생성(model.generate) 없이 지시문만 추가하고 기존과 동일하게 teacher-forcing 단일 forward로 Yes/No logit만 읽는 저비용 버전 | overall: baseline 58.40%, adjswap-hint 57.77%, noun_grounding 58.40% | **noun_grounding이 baseline과 저-명사(1-6개) 구간까지 소수점 단위로 완전 동일 — 무반응으로 폐기.** adjswap-hint도 저조(-0.63pp). 로그 재조사 결과 adjswap-hint는 v6.5 때도 이미 한번 실패했었음(59.24%→58.61%, 5-9절). **결론: "생성 없이 지시문만 추가하는" 프롬프트 nudge는 이 파이프라인에서 효과 없음이 2회 이상 재확인됨** — 진짜 CoT(생성 기반)도 v1/v4 로그에서 이미 시도돼 실패(정확도 하락+9~10배 느림, 5-9절)했던 것으로 뒤늦게 확인 |
| v14_listwise_softmax | Qwen3-VL-8B | v8 레시피(LoRA r=128/alpha=256, LR=5e-5, EPOCHS=5, hard_negative.py K=7 d=1~6) 100% 동일 유지, **loss만** AdaptiveDistanceLoss → ListwiseSoftmaxLoss(1 positive+7 negative를 8-way joint softmax cross-entropy로 취급, distance 가중치 없이 균등)로 교체 | epoch1=0.5651→epoch2=0.5693→epoch3=0.5966→epoch4=0.5966(adj_swap_fail 64→54)→**epoch5=0.5987(최종 정점)** | **v8과 달리 5 epoch 내내 과적합 없이 계속 개선.** **LB 0.90052로 v8(0.89528) 대비 +0.524pp 개선, 현재 1위.** 제출 전 다수결 일치율(93.89%) 기반 예측(~0.8857)은 실제보다 낮게 틀림(4-1절 갱신 참고) — 그래도 이론적 근거(joint softmax의 즉시 self-hardening gradient, v2의 ListNet과 동일 계열이지만 순수 격리는 최초)가 실측으로 확인된 첫 사례 |
| v15_dynamic_hardneg | Qwen3-VL-8B | v14(ListwiseSoftmaxLoss) 그대로 유지 + K 확장 시도. 그리드서치+실제 accelerate 다회 사이클 검증으로 **K=8부터 무조건 OOM 확정**(§5-11) → 설계를 "추가"에서 "교체"로 변경: K는 7 그대로, d1 보너스 슬롯 1개만 매 epoch "모델이 제일 헷갈려한 negative"(추가 forward 없이 그 epoch 학습 로그를 재활용해 기록)로 교체. DDP 4-GPU라 `all_gather_object`로 rank간 관찰 동기화 | epoch1=0.5504→epoch2=0.5840→epoch3=0.5966(v14와 동률)→epoch4=0.5903(하락) | **epoch4에서 v14보다 낮아짐 + mean_hard_score가 epoch마다 계속 상승(5.10→6.89→7.92→8.10, 모델이 뱅크의 negative를 잘 못 죽인다는 신호)** — epoch5 도중 사용자 판단으로 학습 중단, best(epoch3) 체크포인트로 추론만 실행. **v14 대비 819개 중 71개(8.67%) 다르게 예측, 다수결 일치율 92.19%(v14의 94.99%보다 낮음) → 회귀 예측 LB≈0.874로 v14보다 낮을 것으로 추정.** 제출 보류, v14를 현재 최종안으로 유지 |

**v11 vs v12 설계 의도**: 둘 다 "vision encoder를 학습시키면 도움이 되는가"를 검증하되, 실험 변수를 다르게 격리함. v11은 "이미 검증된 안정적 LLM 판단 위에서 vision 개선 효과만 순수 격리", v12는 "LLM과 vision이 함께 적응하며 배우게" — **두 결과 모두 v8을 못 넘어 vision 학습 자체가 도움 안 된다는 결론으로 수렴, 이 방향은 폐기.**

상세 설계 변천사(v2→v4→v5의 hard-negative 커버리지 논리)는 이전 기록 보관: 9절 참고.

### 4-1. val exact-match와 LB 점수가 신뢰성 있게 비례하지 않는다 (★ 중요, 미해결)
v6.5(val 0.6029, LB 0.87260) vs v8(val 0.5924, LB 0.89528) vs v10(val 0.6071, LB 0.88481) vs v12(val **0.6092**, LB 0.89354) 네 버전을 나란히 놓으면 val 순위와 LB 순위가 전혀 일치하지 않는다 — **v6.5/v10/v12 셋 다 "val은 v8보다 좋은데 LB는 v8보다 낮다"는 동일 패턴**. `submission_eda.md` 분석 결과, LB 점수는 val exact-match보다 "여러 모델의 다수결 예측과 얼마나 일치하는가"와 더 강하게 연관되어 있었다(10개 모델 기준 상관계수 0.977).

**단, 2026-07-07 v14로 이 다수결 지표도 완벽하진 않다는 게 확인됨**: v14 제출 전 다수결 일치율(93.89%, v8=94.99%보다 낮음)로 회귀 예측한 LB는 약 0.8857(v8보다 낮을 것으로 예상)이었는데, 실제 LB는 **0.90052로 v8을 오히려 이김**(예측이 +1.5pp 빗나감). 즉 **다수결 일치율은 val보다는 신뢰도가 높지만(방향성은 8/9 케이스에서 맞았음) 여전히 확정적 예측 도구는 아니다** — 표본이 9~10개뿐인 회귀라 개별 예측 오차가 클 수 있음. 결론: 체크포인트 선택 시 val 단독도, 다수결 단독도 전적으로 믿지 말고 참고 신호 중 하나로만 쓸 것. 최종 판단은 여전히 실제 제출(LB)로만 확정됨. (v15도 이 다수결 지표로는 v14보다 낮게 예측됐는데, 이번엔 아직 실제 제출 안 해서 검증 불가 — 위 v15 행 참고.)

### 4-2. v11≈v8 LB 동점은 우연 — 실제로는 35개(4.3%) 예측이 다름
`submission_v11_best.csv`와 `submission_v8_best.csv`를 직접 비교한 결과 819개 중 35개 예측이 다른데도 LB 점수가 소수점 5자리까지 동일(0.89528)했다. 이는 대회 예선 리더보드가 test의 70%만 반영하기 때문에, 그 35개 차이가 공개 구간 안에서 득실 상쇄된 우연으로 확인됨 — v11이 실제로는 no-op이 아니라 뭔가 다르게 예측하지만, 이 특정 공개 분할에서만 순 정답 개수가 우연히 같았던 것. 20개 전체 제출본 pairwise 비교는 `_submission_eda_all_versions_*.csv` 참고.

### v8 상세: LR 상향이 결정적이었다
- **배경**: "LoRA Learns Less and Forgets Less"(Biderman+ 2024, TMLR) 논문을 검토한 결과, 우리 LoRA 설정이 LLM 텍스트 디코더에만 적용되고(vision encoder/projector는 실제 모델 구조 확인 결과 전혀 안 건드림 — target_modules의 q/k/v/o_proj, gate/up/down_proj가 vision의 attn.qkv/attn.proj/mlp.linear_fc1과 이름이 안 겹침), 논문의 "LLM만 LoRA" 시나리오에 정확히 해당함을 확인. 논문은 이 경우 LoRA 최적 LR이 full finetuning보다 한 자릿수 높아야 한다고(IFT 기준 5e-5~5e-4) 실측으로 보고하는데, 우리는 v1→v4에서 근거 기록 없이 2e-5→1e-5로 낮춘 뒤 계속 그 값을 썼음 — 사실상 "LoRA인데 full-finetuning급 LR"을 쓰고 있었던 셈.
- **변경**: LR을 1e-5→5e-5(5배, 논문 권장 범위 하단)로, LoRA rank도 64→128(alpha 128→256, 비율 유지)로 동시에 상향. EPOCHS는 10→5로 줄임(공격적 LR이면 수렴이 빨라질 것으로 예상).
- **결과**: epoch1(0.5504, v6.5 대비 +1.68pp) → epoch2(0.5840, +3.36pp) → epoch3(0.5924, +0.42pp, 격차 축소 시작) → epoch4(0.5735, **v6.5보다 낮아짐**, avg_loss가 0.30→0.24→0.19→0.16으로 너무 빠르게 떨어지는 과적합 패턴) → epoch5는 loss가 0.05~0.15까지 급락해 상황 악화 예상되어 **epoch5 도중 학습 중단**, best_v8(epoch3 체크포인트)로 test set 추론.
- **LB 0.89528**: val 정점(epoch3, 0.5924)이 v6.5의 val 정점(epoch5, 0.6029)보다 낮았음에도 LB 점수는 v6.5(0.87260)보다 크게 높음(+2.268pp) — val exact_match 수치와 LB 점수가 반드시 비례하지 않을 수 있다는 사례. d=1(adj_swap_fail)은 v6.5보다 오히려 약간 나빠졌지만(64 vs 59) other_fail이 크게 개선(130 vs 137)되어 전체 정확도가 순증한 패턴 — d=1에 과도하게 집중하는 게 능사가 아니라는 5-1의 교훈과 일치.
- **교훈**: 이 프로젝트에서 LR=1e-5는 v1 이후 한 번도 제대로 재검토된 적 없는 값이었고, 실제로는 가장 저평가된 하이퍼파라미터였다. rank보다 LR 쪽 레버가 더 크게 작용했을 가능성이 있음(다음에 rank/LR을 분리해서 각각의 기여도를 확인해볼 가치 있음, 지금은 시간상 동시 변경).
- **(이후 진행 상황)**: DoRA/LoRA+/PiSSA/rsLoRA 전부 시도 완료 — 전부 실패/폐기 확정(§4 v9 행 참고).
  noun-loss retry(v10)도 완료 — val은 역대 최고였으나 LB는 v8보다 낮음(§4 v10 행). 최종적으로 v8을 이긴 건
  loss 구조 자체를 바꾼 v14였음(§4 v14 행).

---

## 5. 핵심 발견 · 교훈 (누적 지식, 중요도순)

### 5-1. d=1(인접 스왑)은 여러 방법으로 공략해도 잘 안 풀리는 완고한 약점
- v1 진단(val 399개): d=1 오답이 chance 대비 **1.70x** 과다 (가장 취약), d=5가 1.27x로 2위, d=3이 1.16x로 3위.
  d=2/4/6은 오히려 chance보다 잘 처리(0.6~0.74x).
- v2(TPRU)도 d=1+d=6만 학습해서 d=2~5(19/23개 wrong perm)를 전혀 안 봄 — 이게 v4 재설계의 출발점이 됨.
- v4 학습 곡선(epoch별 adj_swap_fail): 66→66→**60**→60→61 — epoch1~2엔 정체하다 epoch3에서 처음 감소.
  AdaptiveDistanceLoss(EMA 기반)가 **즉각 반응하지 않고 누적 후 지연 효과**를 낸다는 교훈.
- **원인 규명(v6 EDA)**: 인접 프레임의 시각적 유사도가 실제로 더 높음(Cohen's d≈0.3)을 확인했고, 이 신호는
  **해상도와 무관**(32px에서도 448px에서도 동일) — 미세 디테일이 아니라 구도/색감 수준의 "큰 그림" 유사성이라
  해상도 상향으로는 해결이 안 됨. d=1 취약점의 상당 부분은 **구조적으로 잔존할 가능성**이 있음.
- **v11/v12(vision LoRA 실험)에서도 재확인**: v11 최종, v12 epoch1/epoch2 모두 val 실패 사례의 Kendall distance
  분포를 보면 d=1이 다른 거리 대비 압도적으로 많음(v12 epoch1: d1=66 vs d2~6 11~39, epoch2: d1=64 vs d2~6 11~40).
  vision encoder를 추가로 학습시켜도 이 비율 자체는 거의 안 바뀜 — d=1 문제가 vision 표현력 부족보다는
  더 근본적인(구도/색감 수준의 시각적 유사성, 위 항목 참고) 원인일 가능성에 계속 무게가 실림.
  단, `AdaptiveDistanceLoss`의 적응적 가중치(EMA+softmax, temperature=6.0)가 d=1에 실제로 더 큰 가중치를
  주고는 있지만(로그상 w1이 자주 최댓값), 그럼에도 실패 비율이 안 줄어드는 건 temperature가 부족하거나
  애초에 loss 재가중치로는 못 푸는 한계일 수 있음 — temperature를 훨씬 공격적으로 올려보는 실험은
  아직 미시도.

### 5-2. Base 모델 선택이 학습 방식보다 더 결정적이었다 — 그런데 confound가 있었다
- v1→v2(+2.8%p, TPRU 도입)가 지금까지 가장 큰 단일 개선폭이었음.
- **그런데 zero-shot 비교(226380, 같은 24-permutation 형식 기준)로 재검증한 결과**:
  TPRU-7B(26.32%) ≈ 순정 Qwen2.5-VL-7B-Instruct(26.82%) — **거의 동일**.
  → v1→v2의 개선은 TPRU의 RL 특화가 아니라 **Qwen2-VL→Qwen2.5-VL 세대 업그레이드 자체**가 대부분이었을 가능성.
  TPRU의 학습 도메인(로봇/레고/GUI)이 이 대회 도메인(일반 인물 행동)과 안 맞아 RL 특화가 잘 전이 안 된 것으로 보임.
- **Qwen3-VL-8B-Instruct는 RL 특화 없이도 32.83%**로 TPRU/Video-R1(둘 다 RL 특화)보다 훨씬 높음
  → v6에서 base를 Qwen3-VL-8B로 교체한 근거.

### 5-3. TTA(좌우 반전)는 temporal task에서 치명적
- v2.5(-10.3%p 급락)의 주원인. 영상 속 동작 방향(걷는 방향 등)이 flip으로 반전되면 시간 순서 판단 자체가 깨짐.
- **일반화 교훈**: 방향성이 의미를 갖는 task에서는 좌우 반전류 augmentation을 쓰면 안 됨.

### 5-4. 여러 변경을 한 번에 묶으면 원인 분석이 불가능해진다
- v2.5(TTA+pairwise+해상도 동시 변경)의 급락 원인을 아직도 완전히 못 가름.
- v7(폐기, base+loss+해상도 동시 변경)도 VRAM 문제로 무산되며 같은 실수를 반복할 뻔함.
- **v6부터는 "한 번에 검증된 변경만 묶는다"는 원칙 고수**: base 교체(zero-shot으로 사전 검증됨) +
  temperature 조정(실제 로그 데이터로 사전 검증됨) + No_ordering 처리 변경(라벨 정합성 논리로 검증됨)만 포함.

### 5-5. AdaptiveDistanceLoss의 EMA temperature가 너무 보수적이었다
- `dist_weights.csv` 실측 분석: EMA는 **80%의 스텝에서 d=1을 정확히 가장 어렵다고 진단**했지만,
  `WEIGHT_TEMP=2.0`이 너무 낮아서 실제 가중치(w1≈0.19)가 균등(0.167)과 거의 차이가 안 남.
- temperature 6~8로 올리면 d3~6 비중을 61%대(균등 67%에서 소폭만 하락)로 유지하면서 d=1에 의미 있게
  집중(w1≈0.24~0.26) 가능함을 실측으로 확인 → v6에 `WEIGHT_TEMP=6` 반영.
- **"learnable"이 항상 정답은 아니다**: 진짜 gradient 기반 learnable weight(γ)를 시도했다가 수학적으로
  방향이 반대(loss를 낮추려 가중치 자체를 꺼버리는 인센티브)인 버그를 발견 — 자세한 유도는 §9
  "v6(구상 단계, 폐기): learnable γ 가중치" 참고. EMA+수동 튜닝이 미검증 learnable 메커니즘보다
  지금 시점엔 더 안전한 선택.

### 5-6. No_ordering 샘플을 Ordered와 다르게 취급할 근거가 약했다
- 구버전: "No_ordering 샘플은 어떤 순서로 봐도 Yes가 정답"이라는 가정하에 positive만 5배 증강.
- 그런데 실제 라벨은 Answer가 `[1,2,3,4]`로 고정되어 있고, 대회 채점은 exact-match.
  "다른 순서도 맞다"는 건 검증 안 된 가정이었고, 오히려 Ordered와 동일하게 hard-negative 대조 학습시키는 게
  채점 기준과 더 일치함.
- 부수 효과: 그룹 수 7005→1401(80% 감소), 학습 신호도 초반에 saturate 안 되고 지속적으로 유의미해짐,
  코드도 훨씬 단순해짐(특수 분기/`loss_no_ord`/`NO_ORDERING_WEIGHT` 전부 제거).

### 5-7. v5(listwise 생성 방식)가 v4보다 낮았던 이유
Qwen3-VL-8B 기반으로 "24-permutation 검증" 대신 "3,1,2,4" 같은 순열을 SFT로 직접 생성하는 방식을 시도했으나
val 0.4475로 v4(0.5336)에 못 미침. 원인 분석:
1. **검증(verification)이 생성(generation)보다 쉬움** — 후보를 보고 맞는지 판단하는 게, 정답을 무에서
   만들어내는 것보다 근본적으로 쉬운 task. 객관식 vs 주관식과 같은 원리.
2. **학습 신호 밀도 차이** — v4는 대조학습으로 그룹당 8개 순열을 명시적으로 비교하지만, v5는 정답 하나만
   teacher-forcing. 5 epoch 기준 v4가 약 7.5배 많은 대조 인스턴스를 학습.
3. **디코딩 방식** — v4는 24개 전부 채점 후 argmax(완전탐색), v5는 그리디 순차 생성(첫 자리를 잘못 고르면
   되돌릴 수 없음).
- 다만 v5는 학습 속도가 v4보다 13배 빨랐음(그룹당 forward 1회 vs 8회 대조 없음) — 이 속도 이점과
  hard-negative 대조학습을 결합하는 "이미지 1회 인코딩 + 후보 전체 채점" 아이디어를 검토했으나
  Qwen3-VL의 M-RoPE가 KV 캐시 재사용과 호환 안 돼 구현 보류.

### 5-8. TPRU의 GRPO 방식을 그대로 재현하는 것은 기각
- TPRU는 8×GPU, **LoRA 아닌 전체 파라미터** FSDP 파인튜닝, GRPO rollout.n=5(샘플당 5개 완성본 생성),
  최대 1024토큰 CoT 생성, vLLM+Ray 인프라 필요 — 지금까지 써온 LoRA+SFT 파이프라인과 차원이 다른 비용.
- **더 근본적 이유**: RL(GRPO)은 정답을 직접 gradient로 줄 수 없을 때(sparse/미분불가 reward) 쓰는 기법.
  우리는 모든 학습 샘플의 정확한 정답 순열을 이미 갖고 있는 **전형적인 지도학습 상황**이라, SFT/대조학습이
  이미 정답에 대해 직접 gradient를 주는 더 효율적인 방법. RL의 강점(exposure bias 해결)도 우리 타겟 포맷
  (극히 짧고 구조화된 출력)에는 크게 적용 안 됨.

### 5-9. 진짜 생성형 CoT는 이미 v1/v4에서 시도돼 실패했었다 (2026-07-07 로그 재조사로 발견)
- `idea.md`에 기록이 안 남아있던 실험. `v4_tpru_adjpair/logs/test_cot_226419`(진짜 `model.generate()`로
  reasoning 생성 후 Yes/No 판정, n=10 스모크) vs `test_baseline_226420`(CoT 없음): **CoT 40%(4/10) vs
  baseline 50%(5/10), 속도는 CoT가 9~10배 느림**(34.7~52s/sample vs 3.7~4.3s/sample). 실패 사례를 보면
  모델이 "Reasoning: The horse puts its feet down and the camera zooms in.\nAnswer: Yes"처럼 reasoning이
  캡션을 그대로 되풀이하거나 형식만 흉내내고 실제 판단엔 못 씀.
- `v1_qwen2vl_finetune/logs/infer_cot_225615`: 전체 test 819개에 CoT를 돌리려다 **샘플당 90~100초**라
  4개 처리 후 취소(전체로 확장 시 819×95s≈21.6시간으로 24h 추론 한도를 이 실험 하나로 거의 다 쓸 뻔함).
- **결론**: n=10이라 통계적으로 확정적이진 않으나, 정확도 하락 + 압도적으로 느림이 겹쳐 진짜 CoT 계열은
  이 태스크(4장 이미지+짧은 캡션, 24-permutation 전수조사)와 궁합이 안 좋다는 정황이 v13(5-9 nudge형
  실험, 아래)과 별도로 재확인됨. CoT 방향은 완전히 접는 것으로 정리.
- **v13 Tier 0 재확인**: adjswap-hint 프롬프트는 v6.5 때도 이미 실패(59.24%→58.61%, `v6.5_resample/logs/
  prompt_ab_226487.out`)였고, 오늘 v8에서도 재확인(58.40%→57.77%) — 2회 실패로 완전히 폐기 확정.
  n_noun grounding(신규, 생성 없는 nudge형)도 baseline과 저-명사 구간까지 완전 동일(무반응).

### 5-10. 화면 자막/타이틀카드/스코어보드 — EDA에 없던 신규 시각 신호 (2026-07-07 발견)
train 내부 샘플의 이미지를 직접 열어보다가 발견: 일부 영상(주로 튜토리얼/방송 클립)의 프레임에 순서를
알려주는 화면 텍스트(자막, 타이틀카드, 스코어보드 등)가 박혀있어 문장 내용과 매칭되는 경우가 있음 —
반면 비슷한 장르의 다른 샘플엔 전혀 없어 **일부 영상에만 존재하는 sporadic한 특징**으로 확인.

tesseract OCR로 train 내부에서 정량 스캔한 결과, 인식 글자수 임계값에 따라 다르지만 현실적인 "의미
있는 자막 존재" 비율은 대략 5~10%로 추정(짧아도 의미 있는 경우가 있어 글자수만으로 깔끔한 컷은
어려움). 이 비율이 개선 효과의 이론적 상한이라 전체 정확도에 주는 영향은 크지 않을 전망이나(최대 몇
pp), 저비용 실험 후보로 `idea_v13_proposal.md`에 설계 방향 기록.

**설계 방향(미착수)**: VLM이 스스로 흐릿한 자막을 읽게 지시하는 것(5-9의 nudge 실패 전례로 볼 때 리스크)
보다, tesseract로 미리 추출한 텍스트를 프롬프트에 직접 주입하는 방식을 권장. OCR 품질 필터링(짧은 노이즈
문자열 배제) 및 텍스트 감지된 샘플에만 조건부 삽입(90%+ 무관 샘플에 지시 추가 시 attention 분산 우려) 필요.
최종 3090 실행 환경에 tesseract 바이너리 설치 가능 여부도 사전 확인 필요.

### 5-11. K(hard negative 개수) 확장은 이 하드웨어에서 물리적으로 불가능 — 그리고 "단발성 테스트로는
안 잡히는 OOM"이 있다는 교훈 (2026-07-08, v15 설계 과정)
- **배경**: v15에서 "negative를 더 많이 보여주면 좋지 않을까"(K=7→더 크게)를 시도. 처음엔 성긴 그리드
  (n_extra 0/4/8/12/16 × minibatch 8/12/16/20/24)로 스캔했더니 n_extra=0만 살아남고 나머지는 전부 OOM.
- **1단계 정밀화**: minibatch/n_extra를 전부 1단위로(416개 조합) 촘촘히 재스캔 — 여기서 n_extra=1은
  minibatch 전 구간(1~32) 32/32 OK로 나옴(peak 82~83GB). n_extra≥2는 여전히 전부 OOM. 경계가 n_extra=1과
  2 사이에서 깔끔하게 갈림.
- **2단계 함정 발견**: 이 결과를 믿고 K=8(n_extra=1)로 가려 했으나, **forward+backward 단발 테스트는
  AdamW 옵티마이저 상태(momentum+variance) 할당을 포함하지 않는다**는 게 뒤늦게 문제로 떠오름 — 실제
  학습은 GRAD_ACCUM=8 사이클마다 진짜 optimizer.step()이 있고, 첫 호출 때 옵티마이저 상태가 새로
  할당됨. 단발 테스트로는 이 추가 메모리를 절대 못 잡아냄.
- **3단계 진짜 검증**: `accelerator.accumulate(model)`을 train.py와 100% 동일한 패턴으로 써서 진짜
  optimizer.step()이 3번(3 GRAD_ACCUM 사이클) 일어날 때까지 실행하는 별도 스크립트로 재검증.
  **결과: n_extra=0(K=7)은 3사이클 전부 통과(peak 81.43GB), n_extra=1(K=8)은 1번째 사이클(peak
  81.36GB)까지는 통과하지만 2번째 사이클 도중 OOM.** 단일 스텝 테스트에서 "안전"으로 나왔던 K=8이
  실제로는 여러 사이클을 거치면서 서서히 메모리가 차올라 실패하는 걸로 확인됨 — 첫 시도에서 수동으로
  두 n_extra 값을 같은 프로세스/같은 optimizer 인스턴스로 연속 테스트했다가 n_extra=0(원래 안전한 값)
  까지 OOM으로 잘못 나온 적도 있었음(첫 번째 테스트 스크립트의 버그) → 반드시 **독립된 프로세스 +
  실제 학습 코드와 동일한 accelerate 패턴**으로 검증해야 신뢰할 수 있다는 교훈.
- **최종 결론**: K=7(v8/v14와 동일)이 이 하드웨어(H100×4, LoRA r=128, 448px)의 확정된 한계.
  v15는 K를 그대로 두고 "d1 보너스 슬롯을 동적 뱅크로 교체"하는 방식으로 선회(§4 v15 행).
- **일반화 교훈**: (1) VRAM 한계는 "forward+backward 1회"가 아니라 "실제 학습 루프 그대로(옵티마이저
  상태 할당 포함, 여러 GRAD_ACCUM 사이클 반복)"로 검증해야 함. (2) 여러 설정을 같은 프로세스/객체를
  재사용해서 연속 테스트하면 상태 오염으로 없는 문제가 있는 것처럼 보일 수 있음 — 설정마다 독립 프로세스로
  검증할 것.

---

## 6. v6 상세 설계 및 시행착오

### 최종 설정
```
MODEL_PATH   = Qwen3-VL-8B-Instruct
MAX_IMAGE_SIZE = 448
SAMPLE_COUNTS  = {1:2, 2:1, 3:1, 4:1, 5:1, 6:1}   # 전 샘플 동일 적용 (No_ordering 포함)
WEIGHT_TEMP    = 6.0
LORA_R/ALPHA   = 64/128 (변경 안 함 — 근거 부족한 변경 배제)
EPOCHS = 5, BATCH_SIZE=1, GRAD_ACCUM=8, TRAIN_MINIBATCH=8, LR=1e-5
```

### 해상도: 512px 시도 → 실패 → 448px로 복귀
1. VRAM 체크(반복 이미지 사용): 512px+minibatch=8 = 76.80GB(여유 3.2GB) → "감수 가능"으로 판단
2. **실제 데이터로 스모크 테스트하니 OOM 재현**(chunk_size 8→4→2→1까지 다 줄여도 실패) — 반복 이미지 테스트가
   실제 다양한 이미지들의 메모리 사용량을 과소평가했던 것으로 추정
3. 추가 EDA(3절)로 해상도가 d=1 문제 해결에 애초에 도움 안 된다는 것 확인 → **448px 유지가 타협이 아니라
   EDA가 뒷받침하는 결론**으로 정리
4. (스모크 테스트 자체에도 버그 있었음: 그룹 2개를 forward만 몰아서 하고 backward를 한 번에 하면 실제
   학습보다 메모리를 더 씀 — 그룹 1개로 고쳐서 재검증함. 실제 train.py는 BATCH_SIZE=1이라 이 문제 없음)

### 배치 크기: 못 늘림
448px에서 TRAIN_MINIBATCH를 8→16으로 올리는 것도 OOM. Qwen3-VL-8B가 TPRU-7B보다 커서 v4 때보다
여유가 줄어든 것으로 보임. `train.py`에 OOM 자동 chunk 축소 안전장치를 추가해 리스크 완화.

### 속도: 예상보다 빠름
그룹 수 감소(14663→9059, No_ordering 통합 덕분) + flash_attention_2 정상 로드(v4 원본 학습 때는
미설치로 SDPA 폴백했었음) 덕에 실측 약 45분/epoch — 애초 추정(113~120분)보다 훨씬 빠름.
5 epoch + 추론 합쳐 총 4시간 안팎 예상 (24시간 한도에 여유 큼).

### 미해결/확인 필요
- WEIGHT_TEMP=6은 v4(TPRU-7B) 로그로 역산한 값 — Qwen3-VL-8B에서도 동일 패턴일지는 실제 `dist_weights.csv`로 확인 필요
- RTX 3090 단일 GPU 최종 실행 미검증 (2절 체크리스트 참고)

---

## 7. 다음 액션

- [x] v6 학습 완료, epoch1~4 상승(0.5210→0.5693→0.5714→0.5861) 후 epoch5 과적합(0.5735) — v4와 같은 패턴
- [x] v6 최종 결과가 v4(LB 0.83944) 대비 개선인지 판정 → **LB 0.87085로 개선 확인, 제출 완료**
- [x] v6.5(hard negative live 재샘플링, 10epoch) 학습 완료, best epoch5 val=0.6029 (adj_swap_fail 최저 51)
- [x] v6.5 최종 결과가 v6(LB 0.87085) 대비 개선인지 판정 → **LB 0.87260으로 소폭 개선, 제출 완료 (현재 최고)**
- [ ] **⚠️ 주의**: 평가 데이터 특성을 학습 설계 동기로 삼는 것은 규칙 3.4(데이터 누수) 위반
  — v1-실험1/v2가 과거 이 실수를 저지른 전례 있음. train/val 내부 상관관계(r=0.376)만 근거로 정당화할 것.
- [ ] 문장 길이 축 AdaptiveLoss 확장 (train/val 내부 근거만 사용, 위 주의사항 준수) — 우선순위 하향
  (n_nouns가 다중공선성 제거 후 유일한 독립 신호로 확정됨, EDA §10-2 — sent_len 자체 확장은 불필요)
- [x] d=1 겨냥 프롬프트 정교화 → **2회 실패로 폐기 확정** (v6.5 때 59.24%→58.61%, v8/v13에서
  58.40%→57.77%로 재확인, 5-9절). n_noun grounding(신규 nudge형)도 무반응.
- [ ] 체크포인트별(best/last 외 중간 epoch도) 실제 LB 실측 비교 — val-best가 LB-best와 항상 일치하는지 미검증
- [ ] RTX 3090 단일 GPU 최종 실행 검증 (제출 전 필수, 아직 미시도 — 계속 남아있는 최대 리스크)
- [ ] Kaggle 채점이 정말 순수 exact-match인지, 혹시 부분점수 요소가 있는지 최종 확인
- [x] v8(LoRA r=128 + LR=5e-5 + EPOCHS=5) 학습 완료, epoch3(val=0.5924)에서 best, epoch4부터 과적합 확인되어 조기 종료
- [x] v8 최종 결과가 v6.5(LB 0.87260) 대비 개선인지 판정 → **LB 0.89528로 큰 폭 개선(+2.268pp), 제출 완료**
- [x] v9 DoRA/PiSSA/rsLoRA/LoRA+ 계열 전부 시도 → **전부 폐기 확정** (DoRA는 모든 rank에서 OOM,
  weight-norm 고정 오버헤드가 rank 무관하게 VRAM을 거의 다 잡아먹음; PiSSA+rsLoRA+LoRA+ 동시 도입은
  loss 붕괴; α=16 재보정판은 안정적이지만 LB 0.79232로 저조) — LoRA 변형 계열은 당분간 재시도 안 함
- [x] v10_nounloss_retry(v8 레시피 + AdaptiveDistanceNounLoss temp=2.5) 결과 확인 → val 역대 최고(0.6071)
  였으나 **LB 0.88481로 v8보다 낮음**, 폐기
- [x] v11/v12(vision encoder 학습, 두 가지 격리 방식) 결과 확인 → **둘 다 v8 못 넘음, vision 학습 방향
  최종 폐기 확정** (v11 val부터 미달, v12 val은 역대 최고였으나 LB 0.89354로 v8보다 낮음)
- [x] v13(n_noun grounding 프롬프트 nudge) 결과 확인 → **무반응, 폐기** (5-9절)
- [x] v14(ListwiseSoftmaxLoss, v8 레시피 유지 + loss만 교체) 결과 확인 → **과적합 없이 계속 개선,
  LB 0.90052로 v8 대비 +0.524pp 개선, 현재 최고 제출**
- [x] K(hard negative 개수) 확장 시도 → **그리드서치+실제 accelerate 다회 사이클 검증으로 물리적으로
  불가능함을 확정**(K=7이 한계, 5-11절)
- [x] v15(동적 하드 네거티브 뱅크, K=7 유지 + d1 보너스 슬롯만 교체) 학습 완료 → epoch3=0.5966(v14와
  동률) 후 epoch4=0.5903로 하락, mean_hard_score 계속 상승. **다수결 일치율 92.19%(v14의 94.99%보다
  낮음) → LB가 v14보다 낮을 것으로 추정, 제출 보류**
- [ ] **v15를 제출할지 최종 결정** — 다수결 예측을 믿고 보류할지, 그래도 실측 확인 차 제출할지
  (하루 2회 제한 고려해서 신중히 판단)
- [ ] 화면 자막 OCR 프롬프트 실험 (5-10절, tesseract 사전 추출 + 조건부 삽입, 기대 효과는 작지만 저비용)
- [ ] d=1 top-2 tie-break (adjacent pairwise 보조 검증, `idea_v13_proposal.md` Tier 2, 미착수)

---

## 8. 초기 진단 상세 기록 (v1 기준, 2026-07-01, 배경 자료)

> **⚠️ 데이터 누수 관련 안내**: 이 절에는 프로젝트 초기(v1, 2026-07-01)에 평가 데이터 특성을 학습
> 설계에 참고했던 실험(v1-실험1)이 기록되어 있습니다. 이는 규칙 3.4(데이터 누수) 위반 소지가 있는
> 설계였고, 실제로 LB 0.76963으로 성능이 하락해 **즉시 폐기**되었습니다. v3 이후로는 이런 방식을
> 완전히 배제했으며, **최종 제출 모델(v14/v20/v21)의 설계·학습 어디에도 평가 데이터 특성이 관여하지
> 않습니다**(학습 코드 전수 검사로 확인됨 — 평가 데이터는 추론 코드에서만 읽힘). 아래는 그 시행착오를
> 투명하게 남긴 기록이며, 구체적인 평가 데이터 통계는 기재하지 않습니다.

held-out val(399개)에 대해 v1 best checkpoint로 24-permutation 채점 → 정답/예측 간 켄달타우 거리로 오답 분류.

- Val exact-match: 50.1%(200/399), LB(0.79)보다 훨씬 낮음 — train/val 내부만으로는 설명이 부족한 격차
  확인(원인 분석은 이후 버전에서 채점 방식 차이 쪽으로 재확인됨)
- gt_rank≥20(극단 오답) 10개 수동 확인: 대부분 **문장이 짧고 모호함** (예: "She throws the javelin and
  then it is measured.") — 사건을 1~2개만 묘사하는데 4프레임 순서를 다 맞혀야 해서 텍스트만으론 특정 어려움
- 문장 길이 vs 정확도: ≤15단어 26.5% / 35+단어 79.5%, 상관계수 0.376

LLM으로 짧은 문장을 늘리는 augmentation은 검토했으나, 실제 영상 내용과 다른 내용이 삽입될 위험이 있고
생성형 모델 사용 자체가 규칙 위반 소지가 있어 기각.

### 기각된 아이디어: Pairwise 비교 + 정렬 방식 추론
"두 프레임 중 어느 게 먼저냐"는 pairwise 비교를 정렬 알고리즘처럼 모아서 추론하면 24번 대신 훨씬 적은
비교로 충분할 거라는 아이디어. **기각 사유**: pairwise 모델은 두 프레임만 보고 판단해서 문장 전체가
묘사하는 4프레임짜리 global narrative를 학습할 수 없음 (중간에 낀 프레임의 문맥을 못 씀).

### 실패한 실험: 짧은 문장 오버샘플링 (v1-실험1)
`SHORT_SENTENCE_OVERSAMPLE_WEIGHT=1.5`로 20단어 이하 그룹에 가중치 부여 → epoch2에서 중도 제출,
LB 0.76963으로 하락. 원인: (1) 미수렴 상태로 제출, (2) 평가 데이터 특성에 대한 잘못된 가정으로 타겟
자체가 잘못됨, (3) WeightedRandomSampler가 중간/긴 문장 학습 기회를 줄임.

---

## 9. 설계 변천 상세 기록 (v2 → v3 → v4 → v5, 배경 자료)

### v2가 남긴 맹점
4개 원소 순열의 wrong permutation 23개 중 거리 분포는 `d1:3개 d2:5개 d3:6개 d4:5개 d5:3개 d6:1개`.
v2는 d1+d6=4개만 학습, 나머지 19개(d2~5 전체)는 학습 중 한 번도 안 봄.

### v3 실패: 브루트포스의 한계
23개 negative 전부 사용 → 24 samples/group ÷ TRAIN_MINIBATCH=8 = 3 forward/group → 145h(5epoch) 추정,
24h 파티션 한계 초과로 취소.

### 핵심 인사이트: "빼지 말고 더해야 한다"
초기 v4 설계(d1+d6만 남기고 나머지 쳐냄)는 v2와 커버리지가 사실상 동일했음. 진짜 개선은 v2가 못하던
d2~d5 coverage를 **추가**하는 것.

### v4: Distribution-informed Full Coverage
`SAMPLE_COUNTS={1:2,2:1,3:1,4:1,5:1,6:1}` = 8 samples/group = 1 forward pass. AdaptiveDistanceLoss가
매 step마다 distance별 EMA를 추적해 softmax(ema×temperature)로 가중치 산출 — "지금 어디서 틀리고
있는지"에 실시간 적응.

### v5(구, 폐기): 취약 거리 오버샘플
`SAMPLE_COUNTS={1:3,2:3,3:2,4:1,5:1,6:1}` = 12 samples/group = 2 forward pass, v4보다 2배 느림.
d=2를 3개로 늘린 근거가 약했음(d=2는 이미 모델이 잘 처리하는 구간). 결과 검증 전에 Qwen3-VL 기반
listwise 방식(현재 5-7절의 "v5")으로 설계 방향 자체가 바뀌면서 자연 폐기.

### v6(구상 단계, 폐기): learnable γ 가중치
distance별 가중치를 EMA 대신 gradient로 직접 학습(Gradient Reversal + min-max 구조)시키려 했으나,
수학적으로 `∂L/∂γ ≥ 0`이 항상 성립해 gradient descent가 γ를 **줄이는** 방향으로 최적화됨을 발견
(가중치를 키우면 항상 loss가 증가하므로, "어려운 곳에 집중"이 아니라 "가중치 자체를 꺼서 loss를 속이는"
방향으로 학습됨). EMA처럼 `torch.no_grad()`로 gradient를 끊거나 진짜 min-max(양쪽에 반대 부호 업데이트)
구조가 필요한데, 검증 안 된 상태로 구현 리스크가 커서 보류. 현재의 v6(Qwen3-VL-8B 버전)와는 이름만
같고 별개의 아이디어.

---

## 부록: 대회 규칙 전문 (최종 업데이트: 7월 1일 12:00)

1. 참가 자격: 국내외 대학 학부 재학생 또는 휴학생 (전공/학년 무관). 상금은 대표자 명의 국내 계좌로만 지급.
   재학증명서(1개월 이내 발급본) 제출 필요.
2. 참여 규칙: 개인 또는 팀(최대 4인). 신청 후 팀 합치기/나누기 불가(적발시 실격). 다계정 금지.
3. 모델 학습·추론 및 외부 자원 사용 규칙
   - 3.1 Python만 사용, 인터넷 차단된 로컬 환경(CPU/GPU)에서 실행 가능해야 함
   - 3.2 외부 상용 API(ChatGPT 등) 학습/추론 사용 금지. 데이터 전처리 목적 한정 허용, 총 비용 3만원 이하,
     사용 시 발표자료에 명시 필요
   - 3.3 외부 데이터 사용 금지(제공 데이터만). 오픈소스 모델은 2026-05-31 이전 공개 가중치만 허용.
     불확실하면 Kaggle Discussion으로 사전 문의. 모델 앙상블 금지(동일 모델 여러 파인튜닝 후 조합도 금지).
     제공 데이터로 augmentation은 가능하나 **생성형 모델을 이용한 데이터 생성/변형은 금지**.
     경량화(Quantization, LoRA 등) 허용. 추론 전략(CoT, Multi-turn, TTA 등)은 24시간 제한 내 허용.
   - 3.4 데이터 누수 금지: 평가 데이터 수작업 라벨링 후 학습 활용, 평가 데이터 특성 분석 후 학습 설계에
     반영 모두 금지. 위반 시 실격.
   - 3.5 규칙 위반 시 실격 가능, 대회 중에도 재현성 검증 요청 가능
4. 제출 규칙: 1일 2회 제한. 최종 제출 모델은 RTX 3090(24GB) 1개로 실행 가능해야 함.
   서버: AMD EPYC 7502 32-Core×2, 512GB RAM, RTX 3090, driver 550.54.15, CUDA 12.4.
5. 예선 평가: 예선 중 리더보드는 test의 70%(Public)만 반영. 순위는 Exact Match Accuracy
   (완전히 동일한 순서만 정답, 부분점수 없음). 예선 종료 후 전체(Public+Private) 기준 상위팀 코드검증/보고서검토.
6. 코드 재현성 검증: 상위 16위 이내는 학습/추론 코드(.py), 최종 가중치, 보고서(A4 5쪽 이내, MS Word) 제출.
   README.md 상세 작성, 경로는 상대경로, UTF-8 인코딩, 코드는 오류 없이 실행, 개발환경/라이브러리 버전 명시,
   추론 코드는 별도 작성 + 가중치 동봉. **전체 크기 80GB 이하**.
7. 본선 발표 자료 제출: 발표 전날까지 PPT/PDF, 10분 이내.
8. 본선 발표 규칙: 본발표 10분+질의응답 5분, 팀 대표자 발표, 팀원 전체 참석, 공개 발표.

※ 규칙은 수시로 추가될 수 있음 (최종 업데이트: 2026년 7월 1일 12:00)
